<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="multiplexer.lab.takeout">

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/applogo"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/applogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="multiplexer.lab.takeout.SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="multiplexer.lab.takeout.LogInActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name="multiplexer.lab.takeout.SignUpActivity"
            android:label="SignUp"
            android:parentActivityName="multiplexer.lab.takeout.LogInActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".LogInActivity"/>
        </activity>

        <activity
            android:name="multiplexer.lab.takeout.MainActivity"
            android:label="Home" />
        <activity
            android:name="multiplexer.lab.takeout.ItemActivity.ScanQRActivity"
            android:label="Scan Your QR Code"
            android:parentActivityName="multiplexer.lab.takeout.MainActivity">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity"/>
        </activity>

        <activity
            android:name="multiplexer.lab.takeout.ItemActivity.ProfileActivity"
            android:label="Profile"
            android:parentActivityName="multiplexer.lab.takeout.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity"/>
        </activity>

        <activity
            android:name="multiplexer.lab.takeout.ItemActivity.AboutUsActivity"
            android:label="About Us"
            android:parentActivityName="multiplexer.lab.takeout.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity"/>

        </activity>

        <activity
            android:name="multiplexer.lab.takeout.ItemActivity.StoreLocatorActivity"
            android:label="Store Locator"
            android:parentActivityName="multiplexer.lab.takeout.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity"/>
        </activity>

        <activity
            android:name="multiplexer.lab.takeout.ItemActivity.AddReferralActivity"
            android:label="Add Referral"
            android:parentActivityName="multiplexer.lab.takeout.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity"/>
        </activity>

        <activity android:name="multiplexer.lab.takeout.ItemActivity.MenuActivity"
            android:label="Our Menu"
            android:parentActivityName="multiplexer.lab.takeout.MainActivity">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity"/>

        </activity>
    </application>

</manifest>